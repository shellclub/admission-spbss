services:
  app:
    build: .
    ports:
      - "3002:3000"
    # Removed volumes to ensure we use the built image, not local files
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DB_HOST=host.docker.internal
