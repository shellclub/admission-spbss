# ========================================
# EduSched AI - Docker Environment
# ========================================

# Database Configuration (cPanel MySQL)
# Using host.docker.internal to access XAMPP on localhost
DB_HOST=host.docker.internal
DB_USER=root
DB_PASSWORD=
DB_NAME=sport_registration

# Prisma Database URL (required for Prisma ORM)
DATABASE_URL="mysql://root:@host.docker.internal:3306/sport_registration"

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=edusched-ai-production-secret-2024-v3

# Gemini AI API Key
GEMINI_API_KEY=
